# Bot 4 — Allora forecast

Fetches **BTC** price inference from [Allora Labs](https://docs.allora.network/devs/sdk/allora-sdk-ts) and uses it as the forecast.

## Run

```bash
cp .env.example .env
npm install
npm start
```
